{
  "name": "lazymake",
  "version": "1.0.0",
  "description": "A CLI tool for running make file targets.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cli-select": "^1.0.2",
    "makefile-parser": "^1.0.0"
  }
}
